<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teamusic_grupo11">

    <!-- Permiso para reproducir desde Internet (streaming) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permiso recomendado para servicios en primer plano (API 28+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />


    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppNombre_GrupoX">

        <!-- Activity principal (ajusta el nombre si tu MainActivity está en otro paquete) -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>
</manifest>